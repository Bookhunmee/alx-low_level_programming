#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks if int c is a digit
 * @c: integer c
 * Return: 1 if true, otherwise 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c < 58);
	return (1);
	return (0);
}
