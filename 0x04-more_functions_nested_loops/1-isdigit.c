#include "main.h"

/**
 * _isdigit - checks if int c is a digit
 *  @c: integer c 
 *  Return: 1 if true, otherwise 0
 */
int _isdigit(int c)
{
	if  ((c >= 48) && (c <= 57));
	{
		return (1);
	}

	return (0);
}
